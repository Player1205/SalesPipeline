{
  "name": "nexus-bda-crm",
  "version": "1.0.0",
  "description": "Nexus BDA Pipeline & Performance Engine – monorepo root",
  "scripts": {
    "build": "cd client && npm install && npm run build",
    "start": "cd server && npm install && node server.js"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
